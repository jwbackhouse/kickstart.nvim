-- STATUS COLUMN
vim.fn.sign_define('DiagnosticSignError', { text = '', texthl = 'DiagnosticSignError' })
vim.fn.sign_define('DiagnosticSignWarn', { text = '', texthl = 'DiagnosticSignWarn' })
-- Merge line numbers and signs
vim.opt.signcolumn = 'number'
